# 4단계: 병렬성 한계 측정

**목표**: num_envs 스케일링 시 안정적 반복 실험 가능한 표준값 도출.

---

## 1. 실행 여부

- 사용자 환경에서 Isaac Gym 사용 가능. 아래는 **num_envs 스케일링 시 기록할 항목** 및 권장 범위입니다.

---

## 2. 테스트 순서 및 기록 항목

| num_envs | 실행 성공 | GPU 메모리 | CPU 사용량 | iteration 속도 | viewer 없이 안정 | checkpoint 저장 |
|----------|-----------|------------|------------|----------------|------------------|-----------------|
| 128 | ( ) | ___ GB | ___ % | ___ it/s | ( ) | ( ) |
| 256 | ( ) | ___ GB | ___ % | ___ it/s | ( ) | ( ) |
| 512 | ( ) | ___ GB | ___ % | ___ it/s | ( ) | ( ) |
| 1024 | ( ) | ___ GB | ___ % | ___ it/s | ( ) | ( ) |
| 2048 | ( ) | ___ GB | ___ % | ___ it/s | ( ) | ( ) |
| 4096 | ( ) | ___ GB | ___ % | ___ it/s | ( ) | ( ) |

- **명령 예**:  
  `python runner.py --task=position_setpoint_task --num_envs=<N> --headless=True --use_warp=True`
- **모니터링**: `nvidia-smi -l 1`, `htop`, 학습 로그의 시간/iteration 기록.

---

## 3. 권장 범위 (문서 기준)

- **개발/디버깅**: 64 ~ 256  
- **baseline 실험**: 512 ~ 1024  
- **최종 대량 실험**: 1024 ~ 2048  

---

## 4. 결론

- RTX 3090 기준 **표준 num_envs**는 실제 측정 후 위 표를 채워 결정하는 것을 권장함.
