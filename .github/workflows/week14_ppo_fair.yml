name: Week14 PPO fair comparison

on:
  push:
    branches:
      - chatgpt/week14-ppo-20260830
    paths:
      - .github/workflows/week14_ppo_fair.yml
  workflow_dispatch:

permissions:
  contents: read

jobs:
  train:
    name: Fair ${{ matrix.algorithm }}
    runs-on: ubuntu-latest
    timeout-minutes: 50
    strategy:
      fail-fast: false
      matrix:
        algorithm: [ppo, a2c, trpo, dqn, reinforce]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
      - name: Install system packages
        run: |
          sudo apt-get update
          sudo apt-get install -y swig ffmpeg
      - name: Install Python packages
        run: |
          python -m pip install --upgrade pip wheel setuptools
          python -m pip install 'gymnasium[box2d]' stable-baselines3 sb3-contrib 'imageio[ffmpeg]' pandas matplotlib
      - name: Restore benchmark script
        run: |
          base64 -d experiments/week14_ppo/train_week14_fair.py.gz.b64 | gzip -d > /tmp/train_week14_fair.py
          python -m py_compile /tmp/train_week14_fair.py
      - name: Train and evaluate with common budgets
        env:
          SDL_VIDEODRIVER: dummy
        run: |
          python /tmp/train_week14_fair.py \
            --algorithm '${{ matrix.algorithm }}' \
            --seed 42 \
            --output week14_fair_results \
            --total-timesteps 1000000 \
            --max-wall-seconds 1800 \
            --eval-every-steps 50000 \
            --eval-episodes 10 \
            --final-eval-episodes 50 \
            --n-envs 8
      - name: Upload result
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: week14-fair-${{ matrix.algorithm }}
          path: week14_fair_results/${{ matrix.algorithm }}_seed42
          if-no-files-found: warn
          retention-days: 7
